#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

/*! 
	\brief  Translates all characters in a string to lowercase
	\param  s: string to translate
	\return The original string, in lowercase
*/
std::string toLower(const std::string& s);

#endif
